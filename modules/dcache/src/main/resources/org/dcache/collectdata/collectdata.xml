<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder/>
    <context:annotation-config/>

    <bean id="poolManagerStub" class="org.dcache.cells.CellStub">
        <property name="destination" value="${collectdata.service.poolmanager}"/>
        <property name="timeout" value="30000"/>
    </bean>

    <bean id="instance-data" class="org.dcache.collectdata.InstanceData">
        <property name="poolManagerStub" ref="poolManagerStub"/>
        <property name="siteName" value="${collectdata.instance.site-name}"/>
        <property name="latitude" value="${collectdata.instance.location.latitude}"/>
        <property name="longitude" value="${collectdata.instance.location.longitude}"/>
    </bean>

    <bean id="sendData" class="org.dcache.collectdata.SendData">
        <property name="instanceData" ref="instance-data"/>
        <property name="urlStr" value="${collectdata.destination.url}"/>
    </bean>


</beans>